# Full Stack Calculator 🧮

A simple full stack calculator built using React (Frontend) and Node.js + Express (Backend). The frontend sends numbers + operator to the backend API, and the backend calculates the result and sends it back as JSON.

## 🚀 Features
- Add, Subtract, Multiply, Divide
- Backend API handles calculations
- React UI displays result
- Styled calculator interface (CSS)

## 🛠 Tech Stack
Frontend: React (Vite), CSS  
Backend: Node.js, Express.js, CORS  

## 📂 Folder Structure
my-fullstack-app/
├── backend/
│   ├── server.js
│   ├── package.json
│   └── node_modules/
└── frontend/
    ├── src/
    │   ├── App.jsx
    │   ├── App.css
    │   └── main.jsx
    ├── package.json
    └── node_modules/

## ⚙️ Setup Instructions

### 1️⃣ Clone the repository
```bash
git clone <your-repo-link>
cd my-fullstack-app
